<form method="post" action="{{ path('app_workshop_delete_admin', {'workshopId': workshop.workshopId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workshop.workshopId) }}">
	<button class="btn btn-primary" style="background-color:darkred">Delete</button>
</form>
