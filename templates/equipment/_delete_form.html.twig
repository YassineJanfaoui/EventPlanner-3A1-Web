<form style="display: inline-block" method="post" action="{{ path('app_equipment_delete', { 'EquipmentId': item.equipment.EquipmentId is defined ? item.equipment.EquipmentId : equipment.EquipmentId, 'redirect_route': redirect_route ?? 'app_equipment_index' }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ (item.equipment.EquipmentId is defined ? item.equipment.EquipmentId : equipment.EquipmentId)) }}">
	<button class="btn btn-primary" style="background-color:darkred">Delete</button>
</form>
