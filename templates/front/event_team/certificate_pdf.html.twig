<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Certificate of Participation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        
        .certificate-container {
            width: 100%;
            padding: 20px;
            border: 15px solid #0056b3;
            position: relative;
            background-color: #fff;
        }
        
        .certificate-header {
            text-align: center;
            margin-bottom: 20px;
        }
        
        .certificate-title {
            font-size: 36px;
            font-weight: bold;
            color: #0056b3;
            margin-bottom: 10px;
            text-transform: uppercase;
        }
        
        .certificate-subtitle {
            font-size: 24px;
            color: #333;
            margin-bottom: 30px;
        }
        
        .certificate-body {
            text-align: center;
            margin-bottom: 40px;
            font-size: 18px;
            line-height: 1.6;
        }
        
        .team-name {
            font-size: 28px;
            font-weight: bold;
            color: #0056b3;
            margin: 20px 0;
        }
        
        .certificate-footer {
            display: flex;
            justify-content: space-between;
            margin-top: 60px;
        }
        
        .signature-line {
            width: 200px;
            border-top: 1px solid #333;
            margin-top: 10px;
            text-align: center;
        }
        
        .certificate-date {
            text-align: right;
            margin-top: 20px;
        }
        
        .certificate-seal {
            position: absolute;
            bottom: 30px;
            right: 30px;
            width: 100px;
            height: 100px;
            border: 2px solid #0056b3;
            border-radius: 50%;
            text-align: center;
            line-height: 100px;
            font-weight: bold;
            color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="certificate-container">
        <div class="certificate-header">
            <div class="certificate-title">Certificate of Participation</div>
            <div class="certificate-subtitle">This certifies that</div>
        </div>
        
        <div class="certificate-body">
            <div class="team-name">{{ team.TeamName }}</div>
            <p>has successfully participated in</p>
            <div class="event-name" style="font-size: 24px; font-weight: bold; margin: 15px 0;">
                "{{ event.name }}"
            </div>
            <p>with their project titled</p>
            <div style="font-style: italic; font-weight: bold; margin: 15px 0;">
                "{{ event_team.title }}"
            </div>
            <p>We recognize their valuable contribution and commitment to excellence.</p>
        </div>
        
        <div class="certificate-footer">
            <div>
                <div class="signature-line">Event Organizer</div>
            </div>
            <div>
                <div class="signature-line">Event Director</div>
            </div>
        </div>
        
        <div class="certificate-date">
            Issued on: {{ date|date('F d, Y') }}
        </div>
        
        <div class="certificate-seal">OFFICIAL</div>
    </div>
</body>
</html>