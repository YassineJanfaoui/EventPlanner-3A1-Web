<form method="post" action="{{ path('app_event_equipment_delete', {'id': event_equipment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_equipment.id) }}">
	<button class="btn btn-primary">Delete</button>
</form>
