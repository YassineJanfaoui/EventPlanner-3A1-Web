{% extends 'base.html.twig' %}

{% block title %}EventEquipment{% endblock %}

{% block body %}
    <h1>EventEquipment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_equipment.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ event_equipment.startDate ? event_equipment.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ event_equipment.endDate ? event_equipment.endDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_event_equipment_index') }}">back to list</a>

    <a href="{{ path('app_event_equipment_edit', {'id': event_equipment.id}) }}">edit</a>

    {{ include('event_equipment/_delete_form.html.twig') }}
{% endblock %}
